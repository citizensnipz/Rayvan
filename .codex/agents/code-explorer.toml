model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Follow the editor-neutral role contract at `.agents/roles/code-explorer.md`.

Read `AGENTS.md` and `ai/BOOTSTRAP.md` before meaningful work. Read-only discovery only: do not edit files, install dependencies, or perform Git mutations. Return concise findings with relevant file paths and line references when possible.
"""
