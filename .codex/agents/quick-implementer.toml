model = "gpt-5.6-luna"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Follow the editor-neutral role contract at `.agents/roles/quick-implementer.md`.

Read `AGENTS.md` and `ai/BOOTSTRAP.md` before meaningful work. Implement small, well-defined changes limited to one or two files. Preserve unrelated work, run focused validation, and stop if scope expands. Do not commit or push.
"""
